import importDB as imp
import exportDB as exp
import correct as corr

def begin(): # точка входа 
    # нужно меню из 4х пунктов: 

    

    #exp.export_data(data) #переходим в экспорт. что будем передавать в функцию? 
        

    #imp.import_data(data) #переходим в импорт. передаем в функцию файл из которого заливаем данные
        
    # corr.correct_note() # переходим в корректировку. что будем передавать в функцию? 
    return